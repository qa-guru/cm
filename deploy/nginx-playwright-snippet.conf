# Устарело: используйте полный nginx-selenoid.conf
#
# На 443 один location / → selenoid-ui:8080 с auth_basic.
# UI проксирует /wd/hub/ и /playwright/ на локальный hub.
# Прямой hub API с auth — порт 4445 (см. nginx-selenoid.conf).
